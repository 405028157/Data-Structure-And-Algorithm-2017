名词：稀疏图，稠密图，完全图（任意两顶点之间都有边），邻接表，逆邻接表，连通，路径，回路，连通图，连通分量（Components），强连通，强连通图，强连通分量，弱连通（如果把
方向去掉就成连通的图就称为弱连通）

六度空间理论：任何两个人之间，他们可以通过不超过六个人来认识

数据类型定义三要素：
类型名称，数据对象集、操作集

图可以分为无向图和有向图，有向图的边可以是单向的也可以是双向的，总之方向对其来说很重要
带权重的图 —— 网络

对于有N个顶点的无向图，怎样存储可以省一半空间？
正确答案：用一个长度为N(N+1)/2的1维数组

***对于网络，只要把G[i][j]的值定义为边
<vi,vj>的权重即可。
问题：vi和vj之间若没有边该怎么表示？

对于有向图，某点邻接到的点和邻接到该点的点都是邻接点
任一顶点的“度”：（从该点发出的边数为“出度”，指向该点的边数为“入度”）

DFS深度优先搜索(Depth First Search)
  若有N个顶点、E条边，时间复杂度是
   用邻接表存储图，有O(N+E)
   用邻接矩阵存储图，有O(N2)
  
BFS广度优先搜索(Breadth First Search)
  若有N个顶点、E条边，时间复杂度是
 用邻接表存储图，有O(N+E)
 用邻接矩阵存储图，有O(N2)

具有N（N > 0）个顶点的无向图至多有 N 个连通分量
具有N（N > 0）个顶点的无向图至少有 1 个连通分量


如果从无向图的任一顶点出发进行一次深度优先搜索可访问所有顶点，则该图一定是 C
A.有回路的图
B.完全图
C.连通图
D.一棵树







